# 揭棋对弈系统 — 项目总体大纲

## 一、项目概述

基于中国象棋变体"揭棋"的双人真人对弈系统。后端 Java（Spring Boot），前端 Vue3，通过 TCP/WebSocket 自定义协议通信。

### 核心特点
- 棋子开局暗置，移动/吃子后翻面，身份随机由服务器决定
- 士/象翻明后可过河，活动范围大幅增强
- 服务器校验所有着法，自动记录棋谱
- 支持多组客户端互操作（统一通信协议）

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 3.x | 管理 Bean、配置、定时器 |
| 通信层 | Netty (TCP) + STOMP/WebSocket | 原生 TCP 供 Java 客户端；WebSocket 供 Web 前端 |
| 序列化 | JSON (Jackson/Gson) | 前后端统一用 JSON 传输 Move 等对象 |
| 前端框架 | Vue 3 + Vite | Composition API |
| 前端状态 | Pinia | 管理对局状态 |
| 持久化 | H2 (开发) / MySQL (生产) + JPA | 存储用户、棋谱 |
| 构建工具 | Maven | 后端构建 |
| 实时时钟 | java.util.Timer / ScheduledExecutorService | 超时判定 |

---

## 三、系统架构图

```
┌──────────────────────────────────────────────────┐
│                  Vue3 前端                        │
│  ┌─────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ 棋盘组件 │ │ 对局信息  │ │ 棋谱/走子历史     │  │
│  └────┬────┘ └────┬─────┘ └────────┬─────────┘  │
│       └───────────┴───────────────┘             │
│                    │ WebSocket                   │
└────────────────────┼────────────────────────────┘
                     │
┌────────────────────┼────────────────────────────┐
│              Java 后端 (Spring Boot)              │
│                    │                             │
│  ┌─────────────────┴──────────────────┐         │
│  │         通信层 (Protocol)           │         │
│  │  ┌──────────┐  ┌────────────────┐  │         │
│  │  │TCP Server│  │WebSocket Server│  │         │
│  │  │ (Netty)  │  │  (STOMP)       │  │         │
│  │  └────┬─────┘  └───────┬────────┘  │         │
│  └───────┼────────────────┼───────────┘         │
│          └────────┬───────┘                     │
│                   │                              │
│  ┌────────────────┴──────────────────┐         │
│  │         游戏服务层 (Service)        │         │
│  │  ┌──────────┐  ┌────────────────┐  │         │
│  │  │GameService│  │MatchmakingSvc │  │         │
│  │  │ (对局管理) │  │   (匹配撮合)   │  │         │
│  │  └────┬─────┘  └───────┬────────┘  │         │
│  └───────┼────────────────┼───────────┘         │
│          │                │                      │
│  ┌───────┴────────────────┴───────────┐         │
│  │         游戏引擎层 (Engine)          │         │
│  │  ┌──────────┐  ┌──────────────────┐│         │
│  │  │RuleEngine│  │  MoveGenerator   ││         │
│  │  │ (规则校验) │  │  (合法走法生成)   ││         │
│  │  └──────────┘  └──────────────────┘│         │
│  │  ┌──────────┐  ┌──────────────────┐│         │
│  │  │ GameFlow │  │  TimerManager    ││         │
│  │  │ (对局流程) │  │  (超时计时管理)   ││         │
│  │  └──────────┘  └──────────────────┘│         │
│  └────────────────────────────────────┘         │
│                   │                              │
│  ┌────────────────┴──────────────────┐         │
│  │           领域模型层 (Model)        │         │
│  │  ChessPiece / Move / Board / Game │         │
│  │  Player / GameNotation / Position │         │
│  └────────────────────────────────────┘         │
│                   │                              │
│  ┌────────────────┴──────────────────┐         │
│  │        持久化层 (Repository)        │         │
│  │  JPA + H2/MySQL 存储棋谱、用户     │         │
│  └───────────────────────────────────┘         │
└─────────────────────────────────────────────────┘
```

---

## 四、项目目录结构

### 4.1 后端 Java 项目 (`JAVA/`)

```
JAVA/
├── pom.xml
└── src/
    ├── main/java/com/jeiqi/
    │   ├── JeiqiApplication.java          # Spring Boot 启动类
    │   │
    │   ├── model/                          # 【领域模型层】
    │   │   ├── ChessPiece.java             #   棋子（类型/颜色/明暗/位置）
    │   │   ├── Move.java                   #   走子记录（source, dest, type, timestamp）
    │   │   ├── ChessBoard.java             #   棋盘（10×9 网格 + 棋子列表）
    │   │   ├── Game.java                   #   对局（双方、状态、回合、计时）
    │   │   ├── Player.java                 #   玩家（ID/名称/执方/连接）
    │   │   ├── GameNotation.java           #   棋谱（moves 列表 + 结果）
    │   │   ├── Position.java               #   坐标（col: a-i, row: 0-9）
    │   │   ├── PieceType.java              #   棋子类型枚举（将/车/马/炮/兵/士/象）
    │   │   ├── GameStatus.java             #   对局状态枚举
    │   │   └── Side.java                   #   红黑方枚举
    │   │
    │   ├── engine/                         # 【游戏引擎层】
    │   │   ├── RuleEngine.java             #   规则校验（着法合法性、胜负判定）
    │   │   ├── MoveGenerator.java          #   合法走法生成器
    │   │   ├── GameFlow.java               #   对局流程（轮流走子、翻面、终局）
    │   │   ├── TimerManager.java           #   超时计时管理
    │   │   └── RandomPieceAssigner.java    #   暗子随机分配（服务器生成随机数）
    │   │
    │   ├── protocol/                       # 【通信协议层】
    │   │   ├── MessageType.java            #   消息类型枚举
    │   │   ├── GameMessage.java            #   通用消息体
    │   │   ├── MoveMessage.java            #   走子消息
    │   │   ├── GameStateMessage.java       #   局面同步消息
    │   │   └── ProtocolHandler.java        #   协议编解码
    │   │
    │   ├── network/                        # 【网络通信层】
    │   │   ├── TcpGameServer.java          #   TCP Socket 服务器（Netty）
    │   │   ├── TcpClientHandler.java       #   TCP 客户端连接处理
    │   │   ├── WebSocketConfig.java        #   WebSocket 配置
    │   │   └── GameWebSocketHandler.java   #   WebSocket 消息处理
    │   │
    │   ├── service/                        # 【业务服务层】
    │   │   ├── GameService.java            #   对局管理（创建/开始/结束/状态）
    │   │   ├── MatchmakingService.java     #   匹配撮合（等待队列、配对）
    │   │   └── NotationService.java        #   棋谱记录与回放
    │   │
    │   ├── controller/                     # 【REST 接口层】
    │   │   ├── GameController.java         #   对局相关 REST API
    │   │   ├── NotationController.java     #   棋谱查询 API
    │   │   └── UserController.java         #   用户相关 API
    │   │
    │   ├── repository/                     # 【持久化层】
    │   │   ├── GameRepository.java         #   对局记录 JPA
    │   │   ├── NotationRepository.java     #   棋谱 JPA
    │   │   └── UserRepository.java         #   用户 JPA
    │   │
    │   └── config/                         # 【配置】
    │       ├── AppConfig.java              #   应用配置（超时、棋盘参数）
    │       └── CorsConfig.java             #   跨域配置
    │
    └── src/main/resources/
        ├── application.yml                 # Spring Boot 配置
        └── static/                         # （生产时可嵌入前端构建产物）
    │
    └── src/test/java/com/jeiqi/
        ├── engine/RuleEngineTest.java      # 规则引擎单元测试
        ├── engine/MoveGeneratorTest.java   # 走法生成测试
        └── model/ChessBoardTest.java       # 棋盘模型测试
```

### 4.2 前端 Vue3 项目 (`jeiqi-web/`)

```
jeiqi-web/
├── package.json
├── vite.config.ts
├── index.html
└── src/
    ├── main.ts
    ├── App.vue
    │
    ├── views/                              # 页面
    │   ├── HomeView.vue                    #   首页/大厅
    │   ├── GameView.vue                    #   对弈主页面
    │   ├── NotationView.vue                #   棋谱浏览
    │   └── ReplayView.vue                  #   棋谱回放
    │
    ├── components/                         # 组件
    │   ├── board/
    │   │   ├── ChessBoard.vue              #   棋盘主组件（10×9 网格）
    │   │   ├── ChessPiece.vue              #   单个棋子（明/暗两种渲染）
    │   │   └── BoardCoordinates.vue        #   坐标标注（a-i, 0-9）
    │   ├── game/
    │   │   ├── GameInfo.vue                #   对局信息（双方、计时、状态）
    │   │   ├── MoveHistory.vue             #   走子历史列表
    │   │   ├── Timer.vue                   #   倒计时显示
    │   │   └── GameResult.vue              #   对局结果弹窗
    │   ├── lobby/
    │   │   ├── RoomList.vue                #   房间列表
    │   │   ├── CreateRoom.vue              #   创建房间
    │   │   └── MatchButton.vue             #   匹配按钮
    │   └── common/
    │       ├── ConfirmDialog.vue           #   确认弹窗
    │       └── Toast.vue                   #   提示消息
    │
    ├── composables/                        # 组合式函数
    │   ├── useWebSocket.ts                 #   WebSocket 连接管理
    │   ├── useGame.ts                      #   对局状态管理
    │   ├── useBoard.ts                     #   棋盘交互逻辑
    │   └── useTimer.ts                     #   前端计时器
    │
    ├── stores/                             # Pinia 状态
    │   ├── gameStore.ts                    #   对局全局状态
    │   └── userStore.ts                    #   用户信息状态
    │
    ├── services/                           # API 服务
    │   ├── api.ts                          #   HTTP REST 请求
    │   └── socket.ts                       #   WebSocket 消息收发
    │
    ├── types/                              # TypeScript 类型
    │   ├── game.ts                         #   Move, Board, Game 等类型定义
    │   └── message.ts                      #   通信协议消息类型
    │
    └── assets/
        ├── styles/
        │   └── board.css                   #   棋盘样式
        └── images/                         #   棋子图片（可选）
```

---

## 五、领域类设计（核心，≥5 个）

### 5.1 类图关系

```
┌──────────────┐         ┌──────────────┐
│    Player    │         │    Game      │
├──────────────┤         ├──────────────┤
│ - id: String │ 1    *  │ - id: String │
│ - name       │────────▶│ - redPlayer  │
│ - side       │         │ - blackPlayer│
└──────────────┘         │ - board      │
                         │ - status     │
                         │ - moves[]    │
                         │ - startTime  │
                         └──┬───┬───┬──┘
                            │   │   │
                            │   │   └──────────────────┐
                            │   │                      │
              ┌─────────────┘   └──────────┐           │
              │ 1                          │ 1         │ *
     ┌────────▼──────┐          ┌─────────▼─────┐  ┌──▼───────────┐
     │  ChessBoard   │          │  GameNotation │  │    Move      │
     ├───────────────┤          ├───────────────┤  ├──────────────┤
     │ - grid[10][9] │          │ - gameId      │  │ - source     │
     │ - pieces[]    │          │ - moves[]     │  │ - destination│
     │ - captured[]  │          │ - result      │  │ - type       │
     └──┬────────────┘          │ - reason      │  │ - turnStart  │
        │ 1                     └───────────────┘  │   Time       │
        │ *                                        └──────────────┘
   ┌────▼────────┐
   │ ChessPiece  │
   ├─────────────┤
   │ - type      │
   │ - side      │
   │ - revealed  │
   │ - position  │
   │ - alive     │
   └─────────────┘
```

### 5.2 各类核心字段与方法

#### ChessPiece（棋子）
```java
public class ChessPiece {
    private PieceType type;        // 0=将帅,1=车,2=马,3=炮,4=兵卒,5=士,6=象
    private Side side;             // RED / BLACK
    private boolean revealed;      // 是否已翻明
    private Position position;     // 棋盘坐标
    private boolean alive;         // 是否存活

    // 获取当前走子规则（暗子按位置决定，明子按真实身份）
    public MoveRule getMoveRule();
    // 翻面
    public void reveal(PieceType actualType);
}
```

#### Move（走子记录）
```java
public class Move {
    private String source;           // 如 "b3"
    private String destination;      // 如 "b4"
    private Integer type;            // 翻出的棋子类型（第一步），后续可为 null
    private long turnStartTime;      // 回合开始时间戳（毫秒）
    private long serverReceiveTime;  // 服务器收到的时间戳
    private int moveNumber;          // 第几手
    private Side side;               // 走子方
    private boolean isRevealMove;    // 是否为原地翻面操作
}
```

#### ChessBoard（棋盘）
```java
public class ChessBoard {
    private static final int ROWS = 10;    // 0-9
    private static final int COLS = 9;     // a-i
    private ChessPiece[][] grid;           // 棋盘网格
    private List<ChessPiece> redPieces;    // 红方存活棋子
    private List<ChessPiece> blackPieces;  // 黑方存活棋子
    private List<ChessPiece> capturedPieces; // 被吃棋子

    public void initialize();               // 初始化棋盘（随机布置暗子）
    public ChessPiece getPieceAt(Position p);
    public void movePiece(Position from, Position to);
    public void revealPiece(Position p, PieceType type);
    public boolean isPositionValid(Position p);
}
```

#### Game（对局）
```java
public class Game {
    private String id;
    private Player redPlayer;
    private Player blackPlayer;
    private ChessBoard board;
    private GameStatus status;         // WAITING / PLAYING / FINISHED
    private Side currentTurn;
    private List<Move> moveHistory;
    private GameNotation notation;
    private long gameStartTime;
    private int movesWithoutCapture;   // 无吃子回合计数（40 回合判和）
    private int repeatedCheckCount;    // 长将计数

    public void start();
    public MoveResult processMove(Move move);
    public void switchTurn();
    public GameResult checkGameOver();
    public boolean isTimeout(long currentTime);
}
```

#### Player（玩家）
```java
public class Player {
    private String id;
    private String name;
    private Side side;
    private transient Object connection;  // Socket连接引用（不持久化）

    public boolean isRed();
    public boolean isBlack();
}
```

#### GameNotation（棋谱）
```java
public class GameNotation {
    private String gameId;
    private List<Move> moves;
    private String result;           // RED_WIN / BLACK_WIN / DRAW
    private String reason;           // CHECKMATE / TIMEOUT / RESIGN / NO_CAPTURE_DRAW
    private Date gameDate;

    public void addMove(Move move);
    public String exportToText();     // 导出文本格式棋谱
    public static GameNotation importFromText(String text);  // 导入棋谱
}
```

---

## 六、通信协议设计

### 6.1 消息格式（JSON）

所有消息统一封装：
```json
{
  "type": "MESSAGE_TYPE",
  "gameId": "xxx",
  "playerId": "xxx",
  "timestamp": 1716123456789,
  "payload": { ... }
}
```

### 6.2 消息类型

| 消息类型 | 方向 | 说明 |
|----------|------|------|
| `JOIN_QUEUE` | C→S | 加入匹配队列 |
| `MATCH_FOUND` | S→C | 匹配成功，分配对手和执方 |
| `GAME_START` | S→C | 对局开始，发送初始棋盘状态 |
| `MAKE_MOVE` | C→S | 客户端走子 |
| `MOVE_RESULT` | S→C | 走子结果（成功/失败原因/翻子结果） |
| `BOARD_STATE` | S→C | 完整棋盘状态同步 |
| `TURN_NOTIFY` | S→C | 通知轮到哪方走子 |
| `TIMEOUT_WARN` | S→C | 超时警告 |
| `GAME_OVER` | S→C | 对局结束（结果+原因） |
| `CHAT` | C↔S | 聊天消息（可选） |
| `RESIGN` | C→S | 认输 |
| `DRAW_REQUEST` | C↔S | 和棋请求/响应 |
| `ERROR` | S→C | 错误消息 |

### 6.3 MAKE_MOVE 消息 payload 示例

```json
{
  "type": "MAKE_MOVE",
  "gameId": "game-001",
  "playerId": "player-red",
  "timestamp": 1716123456789,
  "payload": {
    "source": "b3",
    "destination": "b4",
    "type": null
  }
}
```

首次走子时 `type` 为 null，服务器随机生成翻子结果；后续走子 type 可省略。

---

## 七、核心业务流程

### 7.1 真人对弈流程

```
客户端A          服务器           客户端B
  │                │                │
  ├─ JOIN_QUEUE ──▶│◀── JOIN_QUEUE ─┤
  │                │                │
  │◀─ MATCH_FOUND ─┤── MATCH_FOUND ─▶│
  │                │                │
  │◀── GAME_START ─┤── GAME_START ──▶│
  │                │                │
  │◀─ TURN_NOTIFY ─┤                │  (通知红方先走)
  │                │                │
  ├─ MAKE_MOVE ───▶│                │
  │                ├─ 校验着法       │
  │                ├─ 翻子(如需)     │
  │                ├─ 记录棋谱       │
  │                ├─ 检查终局       │
  │                │                │
  │◀── MOVE_RESULT ┤── BOARD_STATE ─▶│
  │                │◀─ TURN_NOTIFY ──┤
  │                │                │
  │                │  ...循环...    │
  │                │                │
  │◀── GAME_OVER ──┤── GAME_OVER ──▶│
```

### 7.2 走子校验流程

```
接收 Move
    │
    ▼
┌──────────────┐   否    ┌────────────┐
│ 是否己方回合？ ├──────▶│ 返回 ERROR  │
└──────┬───────┘         └────────────┘
       │ 是
       ▼
┌──────────────┐   否    ┌────────────┐
│ source 是否有 ├──────▶│ 返回 ERROR  │
│ 己方棋子？    │         └────────────┘
└──────┬───────┘
       │ 是
       ▼
┌──────────────┐
│ 是否是原地翻面 │── 是 ──▶ 翻子 → 返回结果
│ 操作？        │
└──────┬───────┘
       │ 否（移动）
       ▼
┌──────────────┐   否    ┌────────────┐
│ 着法是否符合  ├──────▶│ 返回 ERROR  │
│ 走子规则？    │         └────────────┘
└──────┬───────┘
       │ 是
       ▼
┌──────────────┐
│ 执行移动      │
│ 翻子(如需)    │
│ 检查吃子      │
│ 切换回合      │
│ 检查终局      │
└──────────────┘
       │
       ▼
  返回 MOVE_RESULT + BOARD_STATE
```

---

## 八、棋谱记录格式

### 8.1 坐标规则
- 棋盘从上到下：**9 ~ 0**（10 行），9 为黑方底线，0 为红方底线
- 棋盘从左到右：**a ~ i**（9 列）
- 红方（先手）位于下方（0 行），黑方位于上方（9 行）

### 8.2 棋谱文本格式

```
[Game "jeiqi-20240524-001"]
[Red "PlayerA"]
[Black "PlayerB"]
[Result "1-0"]
[Date "2024.05.24"]
[Reason "checkmate"]

1. b3b4,R  (红翻出车)
2. g6g5,C  (黑翻出炮)
3. ...
```

---

## 九、开发迭代计划

| 阶段 | 内容 | 预估产出 |
|------|------|----------|
| **P0 基础** | 领域模型类、棋盘初始化、暗子随机分配 | 6 领域类 + 单元测试 |
| **P1 规则引擎** | 走法生成、着法校验、胜负判定、超时处理 | RuleEngine + MoveGenerator |
| **P2 通信层** | TCP/WebSocket 服务器、消息协议、连接管理 | 两客户端可走子对弈 |
| **P3 前端** | Vue3 棋盘组件、WebSocket 对接、对局界面 | 完整可玩 Web 端 |
| **P4 棋谱与持久化** | 棋谱记录/导出/回放、数据库存储 | 棋谱功能完善 |
| **P5 增强** | 并发多桌、房间系统 | 可选功能 |

---

## 十、配置文件（application.yml）核心项

```yaml
server:
  port: 8080

game:
  tcp-port: 9090
  move-timeout-seconds: 60         # 每步超时
  network-delay-tolerance-ms: 5000 # 网络延迟容忍
  no-capture-draw-rounds: 40       # 无吃子判和回合数
  max-repeated-check: 6            # 长将判负阈值

matchmaking:
  queue-timeout-seconds: 60
```

---

## 十一、关键设计决策

1. **服务器权威**：所有规则校验、随机数生成、时间判定均在服务器端，客户端只做展示和输入
2. **协议兼容性**：TCP/WebSocket 共用同一套 JSON 消息协议，确保不同组客户端可互操作
3. **暗子分配时机**：翻面时由服务器 `RandomPieceAssigner` 实时生成随机数确定身份
4. **前后端共享坐标定义**：统一使用 a-i / 0-9 坐标体系，棋盘方向以上下区分红黑
5. **棋谱与对局解耦**：`GameNotation` 独立于 `Game`，便于导入/导出/回放
