# 揭棋对弈系统 — 接口文档

---

## 一、文档说明

本文档定义揭棋对弈系统各层级之间的信息传递接口标准，涵盖：

| 章节 | 接口边界 | 通信方式 |
|------|----------|----------|
| 二 | 前端 ↔ 后端（WebSocket） | STOMP / JSON |
| 三 | 前端 ↔ 后端（REST） | HTTP / JSON |
| 四 | 通信层 → 服务层 | Java 方法调用 |
| 五 | 服务层 → 引擎层 | Java 方法调用 |
| 六 | 引擎层 → 领域模型层 | Java 方法调用 |
| 七 | 持久化层接口 | JPA Repository |

---

## 二、通信协议接口（前端 ↔ 后端 WebSocket）

### 2.1 通用消息信封

所有 WebSocket 消息统一使用以下 JSON 结构：

```json
{
  "type": "消息类型（见枚举表）",
  "gameId": "对局ID（可选，匹配阶段可为 null）",
  "playerId": "玩家ID",
  "timestamp": 1716123456789,
  "payload": { }
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| `type` | String | 是 | 消息类型标识，取值见 2.2 节 |
| `gameId` | String | 否 | 对局 UUID，大厅状态时为空 |
| `playerId` | String | 是 | 发送方玩家 ID |
| `timestamp` | Long | 是 | 客户端发送时的 Unix 毫秒时间戳 |
| `payload` | Object | 否 | 具体业务数据，结构由 type 决定 |

### 2.2 消息类型枚举

```java
public enum MessageType {
    // 匹配阶段
    JOIN_QUEUE,          // C→S  加入匹配队列
    LEAVE_QUEUE,         // C→S  离开匹配队列
    MATCH_FOUND,         // S→C  匹配成功

    // 对局阶段
    GAME_START,          // S→C  对局开始
    MAKE_MOVE,           // C→S  客户端走子
    MOVE_RESULT,         // S→C  走子结果
    BOARD_STATE,         // S→C  全量棋盘同步
    TURN_NOTIFY,         // S→C  回合通知

    // 对局结束
    GAME_OVER,           // S→C  对局结束
    RESIGN,              // C→S  认输
    DRAW_REQUEST,        // C↔S  和棋请求/响应

    // 系统消息
    TIMEOUT_WARN,        // S→C  超时警告
    ERROR                // S→C  错误消息
}
```

### 2.3 各消息 payload 格式

#### 2.3.1 JOIN_QUEUE（C→S）

```json
{
  "type": "JOIN_QUEUE",
  "gameId": null,
  "playerId": "u-1001",
  "timestamp": 1716123456789,
  "payload": {}
}
```

#### 2.3.2 MATCH_FOUND（S→C）

```json
{
  "type": "MATCH_FOUND",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123460000,
  "payload": {
    "opponentName": "PlayerB",
    "opponentId": "u-1002",
    "assignedSide": "RED",
    "timePerMove": 60
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `opponentName` | String | 对手名称 |
| `opponentId` | String | 对手 ID |
| `assignedSide` | String | 己方执方，取值 `RED` / `BLACK` |
| `timePerMove` | int | 每步限时（秒） |

#### 2.3.3 GAME_START（S→C）

```json
{
  "type": "GAME_START",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123460500,
  "payload": {
    "board": [
      {"pos": "a0", "piece": null, "side": "RED"},
      {"pos": "a1", "piece": null, "side": "RED"},
      {"pos": "a2", "piece": null, "side": "RED"},
      {"pos": "a3", "piece": null, "side": "RED"},
      {"pos": "a4", "piece": null, "side": "RED"},
      {"pos": "a5", "piece": null, "side": "BLACK"},
      {"pos": "a6", "piece": null, "side": "BLACK"},
      {"pos": "a7", "piece": null, "side": "BLACK"},
      {"pos": "a8", "piece": null, "side": "BLACK"},
      {"pos": "a9", "piece": null, "side": "BLACK"}
    ],
    "redPlayerId": "u-1001",
    "blackPlayerId": "u-1002",
    "currentTurn": "RED",
    "timePerMove": 60
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `board` | Array\<Cell\> | 90 个位置的初始棋盘状态 |
| `board[].pos` | String | 坐标，格式 `{a-i}{0-9}` |
| `board[].piece` | String\|null | 暗子时该字段均为 `null`（不透露身份） |
| `board[].side` | String | 棋子属于哪一方 |
| `redPlayerId` | String | 红方玩家 ID |
| `blackPlayerId` | String | 黑方玩家 ID |
| `currentTurn` | String | 当前先手方 |
| `timePerMove` | int | 每步限时（秒） |

#### 2.3.4 MAKE_MOVE（C→S）

```json
{
  "type": "MAKE_MOVE",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123470000,
  "payload": {
    "source": "b3",
    "destination": "b4",
    "type": null
  }
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| `source` | String | 是 | 起始坐标 |
| `destination` | String | 是 | 目标坐标，原地翻面时与 source 相同 |
| `type` | String\|null | 否 | 翻子时传入 `null`，服务器随机分配；明子移动时可省略 |

#### 2.3.5 MOVE_RESULT（S→C）

```json
{
  "type": "MOVE_RESULT",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123470500,
  "payload": {
    "success": true,
    "move": {
      "source": "b3",
      "destination": "b4",
      "side": "RED",
      "moveNumber": 1,
      "isRevealMove": false,
      "revealedPiece": "CHARIOT",
      "capturedPiece": null
    },
    "nextTurn": "BLACK",
    "timeRemaining": 58
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `success` | boolean | 着法是否合法 |
| `errorCode` | String\|null | 失败时的错误码 |
| `errorMessage` | String\|null | 失败时的错误描述 |
| `move.source` | String | 起始坐标 |
| `move.destination` | String | 目标坐标 |
| `move.side` | String | 走子方 |
| `move.moveNumber` | int | 第几手 |
| `move.isRevealMove` | boolean | 是否原地翻面操作 |
| `move.revealedPiece` | String\|null | 翻出的棋子类型（KING/CHARIOT/HORSE/CANNON/PAWN/ADVISOR/ELEPHANT） |
| `move.capturedPiece` | String\|null | 被吃的棋子坐标，无吃子时为 null |
| `nextTurn` | String | 下一手轮到哪方 |
| `timeRemaining` | int | 该方剩余时间（秒） |

#### 2.3.6 BOARD_STATE（S→C）

```json
{
  "type": "BOARD_STATE",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123470600,
  "payload": {
    "board": [
      {"pos": "a0", "piece": "CHARIOT", "side": "RED", "revealed": true, "alive": true},
      {"pos": "a1", "piece": "KING", "side": "RED", "revealed": false, "alive": true}
    ],
    "currentTurn": "BLACK",
    "moveHistory": ["b3b4,R"],
    "redTimeLeft": 590,
    "blackTimeLeft": 600
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `board[].pos` | String | 坐标 |
| `board[].piece` | String | 棋子类型，未翻明时为 `null` |
| `board[].side` | String | 棋子方色 |
| `board[].revealed` | boolean | 是否已翻明 |
| `board[].alive` | boolean | 是否存活 |
| `currentTurn` | String | 当前回合方 |
| `moveHistory` | Array\<String\> | 棋谱简记列表 |
| `redTimeLeft` | int | 红方剩余时间（秒） |
| `blackTimeLeft` | int | 黑方剩余时间（秒） |

#### 2.3.7 TURN_NOTIFY（S→C）

```json
{
  "type": "TURN_NOTIFY",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123471000,
  "payload": {
    "currentTurn": "RED",
    "moveNumber": 5,
    "timeLimit": 60
  }
}
```

#### 2.3.8 GAME_OVER（S→C）

```json
{
  "type": "GAME_OVER",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123600000,
  "payload": {
    "result": "RED_WIN",
    "reason": "CHECKMATE",
    "moveHistory": ["b3b4,R", "g6g5,C"],
    "totalMoves": 42
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `result` | String | 结果：`RED_WIN` / `BLACK_WIN` / `DRAW` |
| `reason` | String | 原因：`CHECKMATE` / `TIMEOUT` / `RESIGN` / `NO_CAPTURE_DRAW` / `REPEATED_CHECK` |
| `moveHistory` | Array\<String\> | 完整棋谱 |
| `totalMoves` | int | 总手数 |

#### 2.3.9 RESIGN（C→S）

```json
{
  "type": "RESIGN",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123500000,
  "payload": {}
}
```

#### 2.3.10 DRAW_REQUEST（C↔S）

```json
{
  "type": "DRAW_REQUEST",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123500000,
  "payload": {
    "action": "REQUEST",
    "requestId": "draw-req-001"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `action` | String | `REQUEST`（发起请求）/ `ACCEPT`（同意）/ `REJECT`（拒绝） |
| `requestId` | String | 和棋请求 ID，用于关联请求-响应 |

#### 2.3.11 ERROR（S→C）

```json
{
  "type": "ERROR",
  "gameId": "g-20240524-001",
  "playerId": "u-1001",
  "timestamp": 1716123470500,
  "payload": {
    "code": "INVALID_MOVE",
    "message": "着法不符合走子规则",
    "source": "b3",
    "destination": "b6"
  }
}
```

### 2.4 走子失败错误码

| 错误码 | 说明 |
|--------|------|
| `NOT_YOUR_TURN` | 非己方回合 |
| `NO_PIECE_AT_SOURCE` | 起始位置无己方棋子 |
| `INVALID_MOVE` | 着法不符合该棋子走子规则 |
| `DESTINATION_OCCUPIED_BY_FRIENDLY` | 目标位置有己方棋子 |
| `KING_FACE_TO_FACE` | 将帅对面 |
| `GAME_NOT_ACTIVE` | 对局已结束或不存在 |
| `TIMEOUT` | 超时判负 |

---

## 三、REST 接口（前端 ↔ 后端 HTTP）

### 3.1 通用规范

- **Base URL**: `http://localhost:8080/api`
- **Content-Type**: `application/json; charset=utf-8`
- **通用响应信封**:

```json
{
  "code": 200,
  "message": "ok",
  "data": { }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | HTTP 状态码，成功为 200 |
| `message` | String | 状态描述 |
| `data` | Object\|null | 业务数据 |

### 3.2 接口列表

#### 3.2.1 用户相关

##### POST /api/user/register

创建新用户。

**Request Body:**
```json
{
  "username": "PlayerA",
  "password": "123456"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| `username` | String | 是 | 用户名，3-20 字符 |
| `password` | String | 是 | 密码，6-32 字符 |

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "id": "u-1001",
    "username": "PlayerA",
    "createdAt": "2024-05-24T10:30:00"
  }
}
```

##### POST /api/user/login

用户登录。

**Request Body:**
```json
{
  "username": "PlayerA",
  "password": "123456"
}
```

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "id": "u-1001",
    "username": "PlayerA",
    "token": "eyJhbGciOi...",
    "stats": {
      "totalGames": 48,
      "wins": 27,
      "losses": 19,
      "draws": 2
    }
  }
}
```

##### GET /api/user/{id}/stats

查询用户战绩。

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "playerId": "u-1001",
    "username": "PlayerA",
    "totalGames": 48,
    "wins": 27,
    "losses": 19,
    "draws": 2,
    "winRate": 0.562
  }
}
```

#### 3.2.2 对局相关

##### GET /api/game/active

查询当前活跃对局列表。

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "games": [
      {
        "gameId": "g-001",
        "redPlayer": "PlayerA",
        "blackPlayer": "PlayerB",
        "status": "PLAYING",
        "moveCount": 12,
        "startTime": "2024-05-24T10:35:00"
      }
    ]
  }
}
```

##### GET /api/game/{gameId}

查询指定对局详情。

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "gameId": "g-001",
    "redPlayer": {"id": "u-1001", "name": "PlayerA"},
    "blackPlayer": {"id": "u-1002", "name": "PlayerB"},
    "status": "FINISHED",
    "result": "RED_WIN",
    "reason": "CHECKMATE",
    "moveCount": 42,
    "startTime": "2024-05-24T10:35:00",
    "endTime": "2024-05-24T10:52:00"
  }
}
```

#### 3.2.3 棋谱相关

##### GET /api/notation/{gameId}

查询对局棋谱。

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "gameId": "g-001",
    "redPlayer": "PlayerA",
    "blackPlayer": "PlayerB",
    "result": "RED_WIN",
    "reason": "CHECKMATE",
    "date": "2024-05-24",
    "moves": [
      {
        "moveNumber": 1,
        "source": "b3",
        "destination": "b4",
        "side": "RED",
        "revealedPiece": "CHARIOT",
        "timestamp": 1716123470500
      },
      {
        "moveNumber": 2,
        "source": "g6",
        "destination": "g5",
        "side": "BLACK",
        "revealedPiece": "CANNON",
        "timestamp": 1716123480000
      }
    ],
    "notationText": "[Game \"g-001\"]\n[Red \"PlayerA\"]\n[Black \"PlayerB\"]\n[Result \"1-0\"]\n[Date \"2024.05.24\"]\n[Reason \"checkmate\"]\n\n1. b3b4,R\n2. g6g5,C"
  }
}
```

##### GET /api/notation/list?playerId={playerId}&page=0&size=10

分页查询玩家棋谱列表。

**Response:**
```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "content": [
      {
        "gameId": "g-001",
        "redPlayer": "PlayerA",
        "blackPlayer": "PlayerB",
        "result": "RED_WIN",
        "reason": "CHECKMATE",
        "date": "2024-05-24",
        "totalMoves": 42
      }
    ],
    "page": 0,
    "size": 10,
    "totalElements": 1,
    "totalPages": 1
  }
}
```

---

## 四、通信层 → 服务层接口

通信层（`network/` + `protocol/`）收到消息后转换为内部 DTO，通过接口调用服务层。

### 4.1 GameMessage（协议层 → 服务层的内部 DTO）

```java
/**
 * 协议层解码后传递给服务层的消息对象。
 * 与 WebSocket 原始 JSON 一一对应但字段已校验并类型化。
 */
public class GameMessage {
    private MessageType type;        // 消息类型枚举
    private String gameId;           // 对局ID，可为 null
    private String playerId;         // 玩家ID
    private long timestamp;          // 客户端时间戳（毫秒）
    private Map<String, Object> payload;  // 原始 payload，由服务层自行反序列化
}
```

### 4.2 服务层方法签名

```java
// ───────── GameService ─────────

/** 处理走子消息，返回结果 */
public MoveResult processMove(String gameId, String playerId, MoveRequest move);

/** 处理认输 */
public GameOverResult handleResign(String gameId, String playerId);

/** 处理和棋请求 */
public DrawResponse handleDrawRequest(String gameId, String playerId, DrawAction action);

/** 获取当前棋盘状态 */
public BoardState getBoardState(String gameId);

/** 获取对局信息 */
public GameInfo getGameInfo(String gameId);

// ───────── MatchmakingService ─────────

/** 玩家加入匹配队列 */
public MatchmakingResult joinQueue(String playerId);

/** 玩家离开匹配队列 */
public void leaveQueue(String playerId);
```

### 4.3 MoveRequest（内部 DTO）

```java
public class MoveRequest {
    private String source;         // 起始坐标，如 "b3"
    private String destination;    // 目标坐标，如 "b4"
    private String type;           // 翻子类型（走暗子时为 null，明子移动省略）
}
```

### 4.4 MoveResult（内部 DTO）

```java
public class MoveResult {
    private boolean success;            // 是否成功
    private String errorCode;           // 错误码（失败时）
    private String errorMessage;        // 错误描述（失败时）
    private Move move;                  // 执行的走子记录
    private BoardState newBoardState;   // 走子后的棋盘状态
    private String nextTurn;            // 下一手方色
    private int timeRemaining;          // 该方剩余时间
}
```

### 4.5 BoardState（内部 DTO）

```java
public class BoardState {
    private Cell[] board;               // 90 个格子的状态数组
    private String currentTurn;         // 当前回合方
    private List<String> moveHistory;   // 走子历史（简记）
    private int redTimeLeft;            // 红方剩余时间（秒）
    private int blackTimeLeft;          // 黑方剩余时间（秒）
}
```

---

## 五、服务层 → 引擎层接口

服务层负责调度，引擎层负责计算。接口以方法签名定义。

### 5.1 RuleEngine 接口

```java
/**
 * 规则引擎：校验着法合法性、判定胜负。
 * 所有规则判定均为无状态方法，仅依赖传入的棋盘快照。
 */
public class RuleEngine {

    /**
     * 校验走子合法性。
     * @param board  当前棋盘
     * @param move   待校验的走子请求
     * @param side   走子方
     * @return 校验结果（合法 / 拒绝原因）
     */
    public RuleResult validateMove(ChessBoard board, MoveRequest move, Side side);

    /**
     * 检查游戏是否终局。
     * @param board  当前棋盘
     * @param game   对局对象（含无吃子计数、长将计数等状态）
     * @return 终局结果，未结束时返回 null
     */
    public GameOverResult checkGameOver(ChessBoard board, Game game);

    /**
     * 判定双方将帅是否对面（将帅对面规则）。
     * @param board  当前棋盘
     * @return 是否对面
     */
    public boolean isKingFaceToFace(ChessBoard board);

    /**
     * 检查目标位置是否有己方棋子。
     */
    public boolean isFriendlyOccupied(ChessBoard board, Position dest, Side side);
}
```

### 5.2 RuleResult（内部 DTO）

```java
public class RuleResult {
    private boolean valid;        // 着法是否合法
    private String errorCode;     // 不合法时返回错误码（NOT_YOUR_TURN / INVALID_MOVE / DESTINATION_OCCUPIED_BY_FRIENDLY）
    private String errorMessage;  // 不合法时的友好描述
}
```

### 5.3 MoveGenerator 接口

```java
/**
 * 合法走法生成器。
 * 根据棋子类型和明/暗状态生成所有合法落点。
 */
public class MoveGenerator {

    /**
     * 生成指定棋子的所有合法走法。
     * @param board  当前棋盘
     * @param piece  目标棋子
     * @return 该棋子所有合法目标位置列表
     */
    public List<Position> generateMoves(ChessBoard board, ChessPiece piece);

    /**
     * 生成某一方的所有合法走法（用于判断是否困毙）。
     * @param board  当前棋盘
     * @param side   目标方色
     * @return 该方所有合法走子（source → [destinations]）
     */
    public Map<Position, List<Position>> generateAllMoves(ChessBoard board, Side side);
}
```

### 5.4 GameFlow 接口

```java
/**
 * 对局流程引擎：管理回合流转、翻子、吃子、终局触发。
 */
public class GameFlow {

    /**
     * 执行一步走子（包含：移动棋子 → 翻面 → 吃子 → 切换回合 → 检查终局）。
     * @param game  对局对象
     * @param move  走子请求
     * @return 执行结果
     */
    public MoveResult executeMove(Game game, MoveRequest move);

    /**
     * 翻面操作（暗子首次移动或原地翻面时触发）。
     * 调用 RandomPieceAssigner 生成随机棋子身份。
     */
    public PieceType revealPiece(ChessBoard board, Position pos, Side side);

    /**
     * 切换回合。
     */
    public void switchTurn(Game game);

    /**
     * 处理认输流程。
     */
    public GameOverResult processResign(Game game, String playerId);
}
```

### 5.5 TimerManager 接口

```java
/**
 * 超时计时管理。
 * 每个对局独立维护一个倒计时器。
 */
public class TimerManager {

    /**
     * 启动对局计时。
     * @param gameId          对局ID
     * @param timePerMove     每步限时（秒）
     * @param onTimeout       超时回调
     */
    public void startTimer(String gameId, int timePerMove, Consumer<String> onTimeout);

    /**
     * 重置计时（走子后调用）。
     */
    public void resetTimer(String gameId);

    /**
     * 停止计时（对局结束时调用）。
     */
    public void stopTimer(String gameId);

    /**
     * 获取剩余时间（秒）。
     */
    public int getRemainingTime(String gameId);
}
```

### 5.6 RandomPieceAssigner 接口

```java
/**
 * 暗子随机分配器。
 * 翻面时根据概率分布随机决定暗子身份。
 */
public class RandomPieceAssigner {

    /**
     * 翻面时随机生成棋子类型。
     * 排除了已经被使用或已被吃掉的类型，保证每种棋子数量符合揭棋规则。
     * @param side          翻面方色
     * @param usedTypes     该方已翻出的棋子类型集合
     * @return 随机分配的棋子类型
     */
    public PieceType assignRandomType(Side side, Set<PieceType> usedTypes);
}
```

### 5.7 GameOverResult（内部 DTO）

```java
/**
 * 终局结果，由引擎层返回给服务层。
 */
public class GameOverResult {
    private String result;        // RED_WIN / BLACK_WIN / DRAW
    private String reason;        // CHECKMATE / TIMEOUT / RESIGN / NO_CAPTURE_DRAW / REPEATED_CHECK
    private String winnerId;      // 胜方 ID，平局时为 null
    private String loserId;       // 败方 ID，平局时为 null
    private List<Move> moveHistory; // 完整棋谱
    private int totalMoves;       // 总手数
}
```

---

## 六、引擎层 → 领域模型层接口

引擎层直接操作领域模型对象，无接口抽象，直接调用模型类方法。

### 6.1 ChessPiece 跨层调用

引擎通过以下模型方法读写棋子状态：

```java
public class ChessPiece {
    /** 获取当前走子规则：暗子按卒（全向一步）行动，明子按真实身份行动 */
    public MoveRule getMoveRule();

    /** 翻面，设置真实身份 */
    public void reveal(PieceType actualType);

    /** 标记阵亡 */
    public void capture();

    // 暴露字段供引擎层读取，但修改须通过方法
    public PieceType getType();
    public Side getSide();
    public boolean isRevealed();
    public Position getPosition();
    public boolean isAlive();
}
```

### 6.2 ChessBoard 跨层调用

```java
public class ChessBoard {
    /** 初始化棋盘（布置 32 枚暗子） */
    public void initialize();

    /** 获取指定位置棋子 */
    public ChessPiece getPieceAt(Position p);

    /** 执行棋子移动（不校验，仅变更位置） */
    public void movePiece(Position from, Position to);

    /** 翻明棋子 */
    public void revealPiece(Position p, PieceType type);

    /** 吃子（从存活列表移入被吃列表） */
    public void capturePiece(Position p);

    /** 坐标合法性检查 */
    public boolean isPositionValid(Position p);

    /** 创建当前棋盘快照（用于规则校验的不可变副本） */
    public ChessBoard snapshot();
}
```

### 6.3 Game 跨层调用

```java
public class Game {
    /** 开始对局 */
    public void start();

    /** 处理走子（委托 GameFlow）*/
    public MoveResult processMove(MoveRequest move);

    /** 切换回合 */
    public void switchTurn();

    /** 检查终局 */
    public GameOverResult checkGameOver();

    /** 判断是否超时 */
    public boolean isTimeout(long currentTime);
}
```

### 6.4 MoveRule（棋子走法策略接口）

```java
/**
 * 棋子的走法规则接口，由各棋子类型分别实现。
 * 暗子统一使用 "暗卒规则"（直行斜行各一步，不可后退）。
 */
public interface MoveRule {
    /**
     * 返回该棋子从 source 出发的所有合法目标位置。
     * @param board  当前棋盘
     * @param source 棋子所在位置
     * @return 合法目标位置列表
     */
    List<Position> getLegalMoves(ChessBoard board, Position source);
}
```

各棋子类型提供各自的实现类，注册在引擎层：
- `KingRule` — 将帅（九宫格内直行一步，不能出九宫）
- `ChariotRule` — 车（直线移动，无子阻挡）
- `HorseRule` — 马（日字形，蹩脚判定）
- `CannonRule` — 炮（直线移动，吃子需翻山）
- `PawnRule` — 兵卒（明子：未过河只能向前，过河可左右；暗子：全向一步）
- `AdvisorRule` — 士（九宫格内斜行一步，翻明后可过河活动）
- `ElephantRule` — 象（田字形，塞眼判定，翻明后可过河活动）
- `HiddenPieceRule` — 暗子（全向一步，不可后退，不可吃对方暗子）

---

## 七、持久化层接口（JPA Repository）

持久化层由 Spring Data JPA 接管，定义 Repository 接口自动生成实现。

### 7.1 GameRepository

```java
public interface GameRepository extends JpaRepository<Game, String> {

    /** 查询玩家参与的对局列表 */
    List<Game> findByRedPlayerIdOrBlackPlayerId(String redId, String blackId, Pageable pageable);

    /** 查询活跃对局 */
    List<Game> findByStatus(GameStatus status);

    /** 按状态和玩家查询 */
    Optional<Game> findByIdAndRedPlayerIdOrBlackPlayerId(String gameId, String redId, String blackId);
}
```

### 7.2 NotationRepository

```java
public interface NotationRepository extends JpaRepository<GameNotation, Long> {

    /** 按对局 ID 查询棋谱 */
    Optional<GameNotation> findByGameId(String gameId);

    /** 查询玩家棋谱列表（按日期倒序） */
    Page<GameNotation> findByGameIdIn(List<String> gameIds, Pageable pageable);
}
```

### 7.3 UserRepository

```java
public interface UserRepository extends JpaRepository<User, Long> {

    /** 按用户名查询 */
    Optional<User> findByUsername(String username);

    /** 检查用户名是否存在 */
    boolean existsByUsername(String username);
}
```

### 7.4 持久化层数据流

```
Service 层
   │ 调用 repository.save(entity) / repository.findById(id)
   ▼
Repository 接口 (Spring Data JPA 代理)
   │ 自动生成 SQL
   ▼
EntityManager / JDBC
   │
   ▼
H2 / MySQL 数据库
```

持久化层不暴露自定义 DTO，直接传递领域模型对象。`GameNotation` 中的 `moves` 通过 JPA `@Convert` 序列化为 JSON 文本存储。

### 7.5 棋谱 JSON 存储格式

```json
{
  "moves": [
    {"source": "b3", "destination": "b4", "side": "RED", "moveNumber": 1, "isRevealMove": false, "revealedPiece": "CHARIOT"},
    {"source": "g6", "destination": "g5", "side": "BLACK", "moveNumber": 2, "isRevealMove": false, "revealedPiece": "CANNON"}
  ],
  "result": "RED_WIN",
  "reason": "CHECKMATE",
  "gameDate": "2024-05-24T10:52:00"
}
```

---

## 八、坐标与名称约定

| 维度 | 约定 | 示例 |
|------|------|------|
| 列 | `a` ~ `i`（从左到右，9 列） | `a`, `e`, `i` |
| 行 | `0` ~ `9`（从上到下，10 行） | `0` 为红方底线，`9` 为黑方底线 |
| 完整坐标 | `{列}{行}` 两个字符 | `b3`, `e0`, `i9` |
| 棋子类型枚举 | `KING`, `CHARIOT`, `HORSE`, `CANNON`, `PAWN`, `ADVISOR`, `ELEPHANT` | |
| 方色枚举 | `RED`, `BLACK` | |
| 对局状态枚举 | `WAITING`, `PLAYING`, `FINISHED` | |
| 对局结果枚举 | `RED_WIN`, `BLACK_WIN`, `DRAW` | |
| 终局原因枚举 | `CHECKMATE`, `TIMEOUT`, `RESIGN`, `NO_CAPTURE_DRAW`, `REPEATED_CHECK` | |

---

*文档版本: v1.0 | 最后更新: 2026-05-26*
