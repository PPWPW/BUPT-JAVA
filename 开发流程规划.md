# 揭棋对弈系统 — 开发流程规划

---

## 总览

```
第1步          第2步          第3步          第4步           第5步          第6步
┌────────┐   ┌────────┐   ┌────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│ 领域模型 │──▶│ 游戏引擎 │──▶│ 持久化层 │──▶│ 业务服务层 │──▶│ 网络通信层 │──▶│ 前端页面  │
│ (Model) │   │(Engine) │   │(Repo)   │   │(Service)  │   │(Network)  │   │(Vue3)    │
└────────┘   └────────┘   └────────┘   └──────────┘   └──────────┘   └──────────┘
     │             │             │              │               │              │
     └─────── 单元测试 ──────────┘              │               │              │
                                                └─── 集成测试 ──┘              │
                                                                              │
                                                                       端到端联调测试
```

**核心原则**：每一层只依赖其下层，不跨层依赖。完成一层 → 单元测试通过 → 进入下一层。

---

## 第一步：领域模型层（Model）

**依赖**：无

**文件**：
```
model/
├── Side.java              # 红黑方枚举
├── PieceType.java         # 棋子类型枚举
├── GameStatus.java        # 对局状态枚举
├── Position.java          # 坐标（col + row）
├── ChessPiece.java        # 棋子实体
├── Move.java              # 走子记录
├── ChessBoard.java        # 棋盘（10×9 网格）
├── Game.java              # 对局
├── Player.java            # 玩家
└── GameNotation.java      # 棋谱
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 1.1 | 创建 `Side`、`PieceType`、`GameStatus` 三个枚举 | 3 个枚举类 |
| 1.2 | 创建 `Position` 类，提供行列互转、相等判断、toString | 坐标工具类 |
| 1.3 | 创建 `MoveRule` 接口（`getLegalMoves` 方法签名） | 策略接口 |
| 1.4 | 创建 `ChessPiece`，实现 `getMoveRule()`、`reveal()`、`capture()` | 棋子实体 |
| 1.5 | 创建 `Move`，包含全部字段和构造器 | 走子记录 |
| 1.6 | 创建 `ChessBoard`，实现 `initialize()`（32 枚暗子放置） | 棋盘类 |
| 1.7 | 创建 `Player`，字段 + `connection` 标记为 transient | 玩家实体 |
| 1.8 | 创建 `GameNotation`，实现 `addMove()`、`exportToText()` | 棋谱类 |
| 1.9 | 创建 `Game`，持有 board、players、moveHistory、状态 | 对局聚合根 |
| 1.10 | 编写 `ChessBoardTest`、`PositionTest` | 单元测试 |

**验证标准**：
- `new ChessBoard().initialize()` 后，棋盘上红方 0-4 行、黑方 5-9 行各 16 枚暗子
- `Position` 坐标互转正确，`a0` → `(col=0, row=0)`，`i9` → `(col=8, row=9)`
- `ChessPiece.reveal(CHARIOT)` 后 `isRevealed() == true` 且 `getType() == CHARIOT`

---

## 第二步：游戏引擎层（Engine）

**依赖**：领域模型层

**文件**：
```
engine/
├── MoveRule.java (接口已在 model 中定义，此处实现)
├── KingRule.java
├── ChariotRule.java
├── HorseRule.java
├── CannonRule.java
├── PawnRule.java
├── AdvisorRule.java
├── ElephantRule.java
├── HiddenPieceRule.java
├── MoveGenerator.java
├── RuleEngine.java
├── GameFlow.java
├── RandomPieceAssigner.java
└── TimerManager.java
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 2.1 | 实现 7 种 `MoveRule` 策略类 + `HiddenPieceRule`（暗子规则） | 8 个规则类 |
| 2.2 | 实现 `MoveGenerator`，组装所有 MoveRule，生成单子/全方合法走法 | 走法生成器 |
| 2.3 | 实现 `RandomPieceAssigner`，根据已翻棋子池按概率分配身份 | 随机分配器 |
| 2.4 | 实现 `RuleEngine.validateMove()`，串联校验逻辑 | 规则校验 |
| 2.5 | 实现 `RuleEngine.checkGameOver()`：将死 / 困毙 / 长将 / 无吃子和棋 | 终局判定 |
| 2.6 | 实现 `GameFlow.executeMove()`：校验 → 移动 → 翻面 → 吃子 → 切换回合 → 终局检查 | 流程引擎 |
| 2.7 | 实现 `TimerManager`：每局独立 Timer，超时回调 | 计时管理 |
| 2.8 | 编写 `RuleEngineTest`、`MoveGeneratorTest` | 单元测试 |

**验证标准**：
- 红车从 `a0` 沿直线可到 `a1`~`a9`、`b0`~`i0`（无子阻挡时）
- 暗子只能走全向一步
- 将帅对面被 `checkGameOver` 检测到
- `assignRandomType` 返回的类型不重复（同方每种棋子有数量上限）

---

## 第三步：持久化层（Repository）

**依赖**：领域模型层（不依赖引擎层，可并行于第二步）

**文件**：
```
repository/
├── GameRepository.java
├── NotationRepository.java
└── UserRepository.java

model/ 中添加：
└── User.java              # 用户实体（JPA @Entity）
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 3.1 | 创建 `User` 实体（id, username, passwordHash, totalGames, wins, losses, draws） | 用户实体 |
| 3.2 | 给 `Game`、`GameNotation`、`User` 添加 JPA 注解 | JPA 映射 |
| 3.3 | 创建 `UserRepository`（findByUsername, existsByUsername） | Repository |
| 3.4 | 创建 `GameRepository`（findByPlayerId, findByStatus） | Repository |
| 3.5 | 创建 `NotationRepository`（findByGameId） | Repository |
| 3.6 | 编写 `application.yml` 配置 H2 数据源和 JPA | 配置文件 |
| 3.7 | 编写 Repository 集成测试（启动 H2，验证 CRUD） | 集成测试 |

**验证标准**：
- `userRepository.save(user)` 后能 `findByUsername` 查到
- `notationRepository.findByGameId("g-001")` 返回正确棋谱
- Spring Boot 启动时 H2 自动建表成功

---

## 第四步：业务服务层（Service）

**依赖**：引擎层 + 持久化层

**文件**：
```
service/
├── GameService.java
├── MatchmakingService.java
└── NotationService.java
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 4.1 | 实现 `GameService`：创建对局 `createGame()`、处理走子 `processMove()`、认输 `resign()`、和棋 `handleDraw()` | 对局服务 |
| 4.2 | 实现 `MatchmakingService`：等待队列 `joinQueue()` / `leaveQueue()`、配对逻辑 | 匹配服务 |
| 4.3 | 实现 `NotationService`：保存棋谱 `saveNotation()`、查询 `getNotation()`、分页 `listNotations()` | 棋谱服务 |
| 4.4 | 编写 Service 层单元测试（Mock Repository 和 Engine） | 单元测试 |

**验证标准**：
- `gameService.processMove()` 校验失败时返回包含错误码的 `MoveResult`
- 两个玩家先后 `joinQueue()` 后匹配成功，返回同一 gameId
- `notationService.getNotation("g-001")` 返回完整棋谱含 `notationText`

---

## 第五步：网络通信层（Network + Controller）

**依赖**：服务层

**文件**：
```
protocol/
├── MessageType.java
├── GameMessage.java
└── ProtocolHandler.java

network/
├── TcpGameServer.java
├── TcpClientHandler.java
├── WebSocketConfig.java
└── GameWebSocketHandler.java

controller/
├── GameController.java
├── NotationController.java
└── UserController.java

config/
├── AppConfig.java
└── CorsConfig.java
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 5.1 | 创建 `MessageType` 枚举、`GameMessage` 通用消息体 | 协议定义 |
| 5.2 | 实现 `ProtocolHandler`：JSON ↔ GameMessage 编解码 | 编解码器 |
| 5.3 | 配置 `WebSocketConfig`（STOMP 端点、Topic 路由） | WebSocket 配置 |
| 5.4 | 实现 `GameWebSocketHandler`：消息分发 → 调用 Service → 回推 | WS 处理器 |
| 5.5 | 实现 `TcpGameServer`（Netty）和 `TcpClientHandler` | TCP 服务器 |
| 5.6 | 实现 `GameController`（REST API：对局查询） | REST 接口 |
| 5.7 | 实现 `NotationController`（REST API：棋谱查询） | REST 接口 |
| 5.8 | 实现 `UserController`（REST API：注册/登录/战绩） | REST 接口 |
| 5.9 | 编写 `CorsConfig` 跨域配置、`AppConfig` 参数配置 | 配置类 |
| 5.10 | 用 WebSocket 客户端工具（Postman/Browser）测试联通 | 连通性测试 |

**验证标准**：
- 启动后端，WebSocket 客户端可连接 `ws://localhost:8080/ws`
- 两个客户端通过 WebSocket 完成匹配 → 走子 → 终局的完整流程
- REST `GET /api/game/active` 返回 JSON 格式正确

---

## 第六步：前端页面（Vue3）

**依赖**：网络通信层（后端接口稳定）

**文件**：
```
jeiqi-web/src/
├── types/game.ts
├── types/message.ts
├── services/api.ts
├── services/socket.ts
├── stores/userStore.ts
├── stores/gameStore.ts
├── composables/useWebSocket.ts
├── composables/useGame.ts
├── composables/useBoard.ts
├── composables/useTimer.ts
├── components/common/Toast.vue
├── components/common/ConfirmDialog.vue
├── components/lobby/RoomList.vue
├── components/lobby/CreateRoom.vue
├── components/lobby/MatchButton.vue
├── components/board/BoardCoordinates.vue
├── components/board/ChessPiece.vue
├── components/board/ChessBoard.vue
├── components/game/Timer.vue
├── components/game/GameInfo.vue
├── components/game/MoveHistory.vue
├── components/game/GameResult.vue
├── views/HomeView.vue
├── views/GameView.vue
├── views/NotationView.vue
├── views/ReplayView.vue
├── App.vue
└── main.ts
```

**工作内容**：

| 序号 | 任务 | 产出 |
|:--:|------|------|
| 6.1 | 搭建 Vite + Vue3 + Pinia + TypeScript 项目骨架 | 项目初始化 |
| 6.2 | 定义 TypeScript 类型（对齐后端 DTO） | `types/` |
| 6.3 | 实现 `api.ts`（HTTP 请求封装）、`socket.ts`（STOMP 客户端） | 通信层 |
| 6.4 | 实现 `useWebSocket`（连接/重连/心跳） | Composable |
| 6.5 | 实现 `userStore`、`gameStore`（Pinia 状态） | Store |
| 6.6 | 实现 `ChessPiece.vue`（明/暗两种渲染） | 棋子组件 |
| 6.7 | 实现 `ChessBoard.vue`（10×9 网格 + 点击交互） | 棋盘组件 |
| 6.8 | 实现 `useBoard`（选择棋子、合法落点高亮） | Composable |
| 6.9 | 实现 `Timer.vue`、`GameInfo.vue`、`MoveHistory.vue` | 对局信息 |
| 6.10 | 实现 `GameResult.vue` 弹窗 | 结算弹窗 |
| 6.11 | 实现 `MatchButton.vue`（匹配/取消） | 匹配按钮 |
| 6.12 | 实现 `useGame`（串联：匹配 → 等待 → 开局 → 走子 → 终局） | 核心逻辑 |
| 6.13 | 实现 `HomeView.vue`（大厅） | 大厅页面 |
| 6.14 | 实现 `GameView.vue`（对弈主页面，组合棋盘+信息+历史） | 对弈页面 |
| 6.15 | 实现 `NotationView.vue`、`ReplayView.vue` | 棋谱页面 |
| 6.16 | 前后端联通调试（WebSocket + REST 完整流程） | 联调 |
| 6.17 | 棋谱回放功能（按 moveHistory 逐步播放） | 回放功能 |

**验证标准**：
- 浏览器打开前端，注册/登录成功
- 两个浏览器窗口分别登录，匹配成功后进入对局
- 暗子显示为背面，移动后翻出随机身份
- 计时器正常倒计时，超时弹结算
- 棋谱回放可逐步展示走子过程

---

## 依赖关系图

```
                    ┌──────────────┐
                    │  第一步: Model │
                    └──────┬───────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              │
     ┌──────────┐   ┌──────────┐         │
     │第二步:    │   │第三步:    │         │
     │Engine    │   │Repository│         │
     └────┬─────┘   └────┬─────┘         │
          │              │               │
          └──────┬───────┘               │
                 ▼                       │
          ┌──────────┐                  │
          │第四步:    │                  │
          │Service   │                  │
          └────┬─────┘                  │
               │                        │
               ▼                        │
        ┌──────────┐                    │
        │第五步:    │                    │
        │Network    │                    │
        └────┬─────┘                    │
             │                          │
             ▼                          │
      ┌──────────┐                      │
      │第六步:    │◀─────────────────────┘
      │Frontend   │   后端 API 服务于前端
      └──────────┘
```

> 第二步（引擎）和第三步（持久化）可以并行开发，因为它们都只依赖第一步（模型），互不依赖。

---

## 里程碑检查点

| 阶段 | 检查项 | 通过标志 |
|------|--------|----------|
| M1 | 6 个领域类 + 3 个枚举完成，单元测试全部通过 | `mvn test` 绿色 |
| M2 | 引擎层完成，8 种棋子走法 + 校验 + 终局判定通过测试 | `RuleEngineTest` / `MoveGeneratorTest` 全部通过 |
| M3 | H2 数据库自动建表，Repository CRUD 正常 | Spring Boot 启动无报错 |
| M4 | 服务层完成，模拟两个玩家走完一局 | Mock 测试覆盖全部流程 |
| M5 | WebSocket 连通，两个客户端可对弈 | 手动用客户端工具走完一局 |
| M6 | 前端完整可玩，Web 端双人对弈 | 两个浏览器标签页完整对弈 |
